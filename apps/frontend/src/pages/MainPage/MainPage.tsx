import { Grid } from '../../components/Grid';

export const MainPage: React.FC = () => {
  return (
    <Grid.Container>
      <Grid.Row>
        <Grid.Column>Main Page</Grid.Column>
      </Grid.Row>
    </Grid.Container>
  );
};
